# App Review Insights Agent 验收测试报告

## 1. 测试概览

- 测试时间：2026-07-19 23:34-23:58（Asia/Shanghai）
- 测试环境：Windows 11，PowerShell，Microsoft Edge / Chromium 内核浏览器
- Node.js 版本：v24.18.0
- npm 版本：11.16.0
- 测试仓库：https://github.com/BayesLee/app-review-insights
- 运行验收 commit：`8dbfe93 test: add report export coverage`
- 项目阶段：提交前最终文档整理与完整验收
- 测试方式：GitHub 远端检查、全新克隆、命令行构建、API 请求、浏览器页面检查、自动化测试

说明：本报告对应的可运行代码验收对象为 `8dbfe93`。本报告自身提交不改变业务代码。

## 2. 环境与仓库检查

### ACC-001 GitHub 仓库可访问性

- 测试场景：检查 GitHub 仓库是否可访问。
- 操作步骤：打开 `https://github.com/BayesLee/app-review-insights`，并执行 `git clone`。
- 预期结果：仓库可访问，默认分支为 `main`。
- 实际结果：仓库页面可访问；`git clone` 成功；克隆后分支为 `main`。
- 是否通过：通过
- 备注：GitHub 页面显示仓库为 Public。

### ACC-002 仓库关键文件检查

- 测试场景：检查面试官首次打开仓库时需要看到的文件。
- 操作步骤：在 fresh clone 中检查 `README.md`、`docs/需求文档.md`、`docs/PRD.md`、`sample_data/reviews.json`、`sample_data/reviews.csv`、`sample_outputs/example-analysis.json`、`package.json`、`package-lock.json`。
- 预期结果：所有文件存在。
- 实际结果：所有文件均存在。
- 是否通过：通过
- 备注：`docs/验收测试报告.md` 将由本次报告提交新增。

### ACC-003 敏感文件与构建目录检查

- 测试场景：确认仓库未跟踪敏感或构建文件。
- 操作步骤：执行 `git ls-files | Select-String -Pattern "(^|/)(node_modules|\\.next|\\.env|\\.env\\.local)(/|$)"`。
- 预期结果：无输出。
- 实际结果：无输出。
- 是否通过：通过
- 备注：fresh clone 后复制的 `.env.local` 为本地未跟踪文件。

## 3. 全新克隆与命令验证

### ACC-004 全新克隆

- 测试场景：模拟面试官第一次获取仓库。
- 操作步骤：在临时目录执行 `git clone https://github.com/BayesLee/app-review-insights.git`。
- 预期结果：克隆成功。
- 实际结果：克隆成功，最新 commit 为 `8dbfe93`。
- 是否通过：通过
- 备注：临时路径为 `C:\Windows\TEMP\app-review-insights-finalcheck-20260719235637\app-review-insights`。

### ACC-005 npm install

- 测试场景：全新安装依赖。
- 操作步骤：在 fresh clone 中执行 `npm install`。
- 预期结果：依赖安装成功。
- 实际结果：安装成功，退出码为 0，安装 74 个 packages。
- 是否通过：通过
- 备注：npm 输出 warning：`sharp@0.34.5` 的 install script 未授权。该 warning 未阻塞安装、测试或构建。

### ACC-006 TypeScript 类型检查

- 测试场景：检查类型是否通过。
- 操作步骤：执行 `npm run typecheck`。
- 预期结果：退出码为 0。
- 实际结果：退出码为 0。
- 是否通过：通过
- 备注：无类型错误。

### ACC-007 自动化测试

- 测试场景：运行项目自动化测试。
- 操作步骤：执行 `npm test`。
- 预期结果：测试全部通过。
- 实际结果：7 个测试文件通过，34 个测试通过。
- 是否通过：通过
- 备注：覆盖评论清洗、JSON/CSV 导入、AI 主题校验、PRD 校验、测试用例生成、追溯完整率和导出内容。

### ACC-008 生产构建

- 测试场景：检查项目是否可构建。
- 操作步骤：执行 `npm run build`。
- 预期结果：Next.js 构建成功。
- 实际结果：构建成功，退出码为 0。
- 是否通过：通过
- 备注：生成路由 `/`、`/_not-found`、`/api/analyze`、`/api/sample-analysis`。

## 4. 开发服务与页面检查

### ACC-009 环境变量模板复制

- 测试场景：按 README 复制 `.env.example`。
- 操作步骤：执行 `Copy-Item .env.example .env.local`。
- 预期结果：`.env.local` 创建成功且不进入 Git 状态。
- 实际结果：`.env.local` 创建成功，`git status` 未显示该文件。
- 是否通过：通过
- 备注：模板中 `OPENAI_API_KEY` 为空。

### ACC-010 开发服务启动

- 测试场景：启动本地开发服务。
- 操作步骤：执行 `npm run dev -- --hostname 0.0.0.0 --port 3000`。
- 预期结果：服务启动成功。
- 实际结果：Next.js `Ready in 12.4s`。
- 是否通过：通过
- 备注：README 标准命令是 `npm run dev`；验收中增加 hostname 参数是为了同时测试 `127.0.0.1` 和 `localhost`。

### ACC-011 访问地址

- 测试场景：检查浏览器访问地址。
- 操作步骤：请求 `http://127.0.0.1:3000` 和 `http://localhost:3000`。
- 预期结果：均返回 200。
- 实际结果：两个地址均返回 200。
- 是否通过：通过
- 备注：Next.js dev server 对 `127.0.0.1` 访问 `_next/*` 资源输出 cross origin warning，不影响页面运行。

### ACC-012 首页展示

- 测试场景：浏览器打开首页。
- 操作步骤：在浏览器访问 `http://127.0.0.1:3000`。
- 预期结果：首页显示输入区、流程状态和结果区。
- 实际结果：页面显示 App 评论洞察 Agent、上传文件、加载示例数据，以及基础统计、AI 模型分析结果、版本规划、PRD、测试用例、追溯关系、导出报告等区域。
- 是否通过：通过
- 备注：浏览器 console error 数量为 0。

## 5. 数据输入与异常场景

### ACC-013 App Store 流程

- 测试场景：使用真实 App Store 链接分析。
- 操作步骤：POST `/api/analyze`，输入 `https://apps.apple.com/us/app/workout-for-women-home-gym/id839285684`，`maxPages=1`，不配置 API Key。
- 预期结果：评论采集和清洗可运行，AI 阶段提示 API Key 缺失或模型错误，页面不白屏。
- 实际结果：返回 200；数据来源为 App Store；原始评论 50；清洗后评论 50；AI 主题状态为 `error`；版本规划和测试用例为 `skipped`。
- 是否通过：通过
- 备注：无 API Key 是预期条件。

### ACC-014 JSON 导入

- 测试场景：导入 `sample_data/reviews.json`。
- 操作步骤：POST `/api/analyze`，`importSource.type=json-file`，content 使用样例 JSON 文件内容。
- 预期结果：文件可解析，数据来源为 JSON，清洗和后续流程可运行。
- 实际结果：返回 200；数据来源为 JSON 文件；原始 12 条；清洗后 11 条；重复 1 条；无 API Key 下 AI 为 `error`，后续为 `skipped`。
- 是否通过：通过
- 备注：文件选择器真实弹窗未自动化执行，但后端导入链路和页面上传控件均已验证。

### ACC-015 CSV 导入

- 测试场景：导入 `sample_data/reviews.csv`。
- 操作步骤：POST `/api/analyze`，`importSource.type=csv-file`，content 使用样例 CSV 文件内容。
- 预期结果：CSV 可解析，字段映射正确。
- 实际结果：返回 200；数据来源为 CSV 文件；原始 12 条；清洗后 11 条；重复 1 条。
- 是否通过：通过
- 备注：字段 `id,rating,title,content,author,date,version` 映射正常。

### ACC-016 空输入

- 测试场景：不输入 App Store 链接，也不上传文件。
- 操作步骤：POST `/api/analyze`，`appUrl=""`，无 `importSource`。
- 预期结果：返回可理解错误。
- 实际结果：返回 400，错误为“请在 App Store 链接和 JSON/CSV 文件上传中二选一。”
- 是否通过：通过
- 备注：页面不会白屏。

### ACC-017 普通网页链接

- 测试场景：输入非 App Store 链接。
- 操作步骤：POST `/api/analyze`，`appUrl=https://example.com`。
- 预期结果：提示无法解析 App ID。
- 实际结果：返回 400，错误为“未能从 App Store 链接中解析出 app id，请确认链接包含 id 后面的数字。”
- 是否通过：通过
- 备注：属于链接错误提示。

### ACC-018 非法 JSON

- 测试场景：上传非法 JSON。
- 操作步骤：POST `/api/analyze`，`importSource.type=json-file`，content 为 `not json`。
- 预期结果：显示 JSON 格式错误。
- 实际结果：返回 400，错误为“JSON 文件格式错误，无法解析评论数据。”
- 是否通过：通过
- 备注：页面不会白屏。

### ACC-019 缺少 content 字段

- 测试场景：JSON 评论有标题但没有 content。
- 操作步骤：上传 `[{"id":"m1","rating":1,"title":"missing"}]`。
- 预期结果：评论被删除并记录为空内容。
- 实际结果：返回 200；原始 1 条；清洗后 0 条；`emptyCount=1`；notes 中提示“content 为空”。
- 是否通过：通过
- 备注：验收中发现并修复了此问题。

### ACC-020 rating 为 0、6 或字符串

- 测试场景：非法评分。
- 操作步骤：分别上传 rating 为 0、6 和 `"bad"` 的评论。
- 预期结果：评论被过滤并提示 rating 必须为 1-5。
- 实际结果：均返回 200；清洗后 0 条；`malformedCount=1`；notes 中提示“rating 必须为 1-5”。
- 是否通过：通过
- 备注：后续阶段跳过。

### ACC-021 空评论内容

- 测试场景：content 为空字符串。
- 操作步骤：上传 `[{"id":"e1","rating":1,"title":"","content":"   "}]`。
- 预期结果：评论被过滤。
- 实际结果：返回 200；清洗后 0 条；`emptyCount=1`；notes 中提示“content 为空”。
- 是否通过：通过
- 备注：页面可显示清洗报告。

### ACC-022 全部重复评论

- 测试场景：上传两条相同评论。
- 操作步骤：上传两条作者、标题、正文和评分相同的 JSON 评论。
- 预期结果：保留 1 条，重复计数为 1。
- 实际结果：返回 200；原始 2 条；清洗后 1 条；`duplicateCount=1`。
- 是否通过：通过
- 备注：无 API Key 下 AI 阶段为 error。

### ACC-023 没有低评分评论

- 测试场景：上传 4-5 星评论。
- 操作步骤：上传 2 条高评分 JSON 评论。
- 预期结果：基础统计可运行，AI 主题发现跳过。
- 实际结果：返回 200；清洗后 2 条；AI 主题状态为 `skipped`；后续阶段 `skipped`。
- 是否通过：通过
- 备注：符合低评分主题发现条件。

## 6. AI、示例缓存与导出

### ACC-024 无 API Key

- 测试场景：`.env.local` 中 `OPENAI_API_KEY` 为空。
- 操作步骤：加载示例数据并点击开始分析。
- 预期结果：基础处理可运行，AI 区域显示明确提示，页面不白屏。
- 实际结果：页面显示“未配置 OPENAI_API_KEY”；后续阶段跳过；可点击“查看示例分析结果”。
- 是否通过：通过
- 备注：浏览器 console error 数量为 0。

### ACC-025 示例缓存结果

- 测试场景：加载缓存示例分析结果。
- 操作步骤：点击“查看示例分析结果”，并请求 `/api/sample-analysis`。
- 预期结果：返回完整示例结果，并明确标注不是实时模型输出。
- 实际结果：接口返回 200；`isSampleOutput=true`；`sampleNotice=示例缓存结果，不是本次实时模型输出。`；页面显示测试用例和 100% 追溯完整率。
- 是否通过：通过
- 备注：缓存结果包含 3 条测试用例。

### ACC-026 模型 API 请求失败

- 测试场景：模拟模型服务不可用。
- 操作步骤：在 fresh clone 的未跟踪 `.env.local` 中临时设置 fake API Key 和 `OPENAI_BASE_URL=http://127.0.0.1:9/v1`，重启服务并请求 JSON 示例分析。
- 预期结果：模型阶段失败但页面/API 不白屏，后续阶段跳过。
- 实际结果：返回 200；`issueDiscovery.status=error`；错误为 `Connection error.`；版本规划和测试用例为 `skipped`。
- 是否通过：通过
- 备注：临时 fake key 未提交。

### ACC-027 模型返回非法 JSON

- 测试场景：模型输出非法 JSON。
- 操作步骤：运行自动化测试中的 mock model 返回 `not json`。
- 预期结果：对应阶段返回可理解错误。
- 实际结果：自动化测试通过，主题、PRD 和测试生成模块均覆盖非法 JSON。
- 是否通过：通过
- 备注：该场景通过单元测试验证，未通过真实 API 模拟。

### ACC-028 真实模型流程

- 测试场景：使用真实 API Key 完成 AI 主题、版本规划、PRD、测试用例、追溯和导出。
- 操作步骤：检查当前环境是否存在真实 API Key。
- 预期结果：若有真实 API Key，则执行完整实时模型流程。
- 实际结果：当前环境变量无 `OPENAI_API_KEY`，fresh clone `.env.local` 也无非空 Key。
- 是否通过：未执行
- 备注：未能完成真实模型测试，原因是当前环境没有可用真实 API Key；未伪造结果。

### ACC-029 报告导出

- 测试场景：检查 JSON/Markdown 报告导出。
- 操作步骤：浏览器确认“导出 JSON 报告”和“导出 Markdown 报告”按钮存在；自动化测试调用 `buildJsonReport` 和 `buildMarkdownReport`。
- 预期结果：报告包含数据来源、分析目标、模型名称、主题、版本规划、PRD、测试用例、追溯链，不包含 API Key。
- 实际结果：导出按钮存在；自动化测试通过，报告内容包含关键结构，不包含 `OPENAI_API_KEY` 和 `sk-test`。
- 是否通过：通过
- 备注：Codex in-app browser 不支持下载事件，因此未能自动捕获真实下载文件；内容生成逻辑已自动化验证。

## 7. GitHub 与最终 Git 检查

### ACC-030 Git remote

- 测试场景：检查远端地址。
- 操作步骤：在本地仓库执行 `git remote -v`。
- 预期结果：origin 指向 `https://github.com/BayesLee/app-review-insights.git`。
- 实际结果：origin fetch/push 均指向该地址。
- 是否通过：通过
- 备注：远端 main 已可 clone。

### ACC-031 git status

- 测试场景：检查提交前工作区状态。
- 操作步骤：执行 `git status`。
- 预期结果：提交后 working tree clean。
- 实际结果：本报告提交前有文档待提交；最终提交后需再次确认。
- 是否通过：待最终确认
- 备注：最终检查见提交后的 `git status`。

### ACC-032 git log

- 测试场景：检查提交历史。
- 操作步骤：执行 `git log --oneline -10`。
- 预期结果：能看到文档、测试和修复提交。
- 实际结果：已看到 `8dbfe93`、`eca90b4`、`23a93b5`、`da15ac3` 等提交。
- 是否通过：通过
- 备注：本报告提交后会新增 `test: complete fresh-clone acceptance verification`。

## 8. 发现的问题

1. 导入数据 warning 没有在页面展示。
   - 影响：非法 rating、空 content 等问题不够直观。
   - 修复：将导入/采集 warning 加入 `scope.notes` 并在“分析范围”显示。
   - 修复 commit：`23a93b5 fix: resolve final acceptance issues`

2. `content` 缺失但 `title` 存在时，评论未被过滤。
   - 影响：不满足“content 为空的数据删除”的要求。
   - 修复：清洗逻辑改为正文 `body` 必须非空，并补自动化测试。
   - 修复 commit：`eca90b4 fix: remove imported reviews with empty content`

3. 导出内容缺少自动化测试覆盖。
   - 影响：无法严格验证导出报告不包含 API Key。
   - 修复：新增 `src/lib/report/export.test.ts`。
   - 修复 commit：`8dbfe93 test: add report export coverage`

## 9. 已修复的问题

- README 全面中文化。
- 新增 `docs/需求文档.md`。
- 新增 `docs/PRD.md`。
- 新增 `sample_data/reviews.csv`。
- 页面显示导入 warning。
- 清洗规则删除正文为空的评论。
- 自动化测试覆盖导出报告内容。

## 10. 未解决限制

- 真实模型测试未执行，因为当前环境没有真实 API Key。
- Codex in-app browser 不支持下载事件，未自动捕获真实下载文件。
- App Store 公共接口存在字段和网络不稳定风险。
- App Store 评论版本号可能无法可靠获取。
- 当前为本地 Demo，未接数据库、登录、部署和权限系统。
- 未做大规模性能测试。
- 模型质量依赖实际模型和配置。

## 11. 最终结论

从“面试官第一次打开仓库”的角度，项目可以被全新克隆、安装、类型检查、自动化测试、构建和本地启动。App Store、JSON、CSV、示例缓存、无 API Key、错误输入和模型 API 失败路径均已验证。除真实模型流程因缺少 API Key 未执行、真实下载文件捕获受浏览器工具限制外，当前仓库满足本地 Demo 提交要求。
